# PRD — NACHI ENG LTD Website Rebuild

## Original problem statement
Full rebuild of the NACHI ENG LTD marketing website (industrial engineering & technology, Southend-on-Sea, Essex; Company No. 16567818). Phased, approval-gated build per the client's master prompt: strict claims discipline (no invented stats, clients, testimonials, accreditations), WCAG 2.2 AA, SEO + performance budgets, Resend-delivered enquiry form (disabled until delivery test passes), no analytics at launch, existing live site at nachieng.co.uk untouched, Emergent preview only. Client's own logo to be uploaded and assessed — no replacement without approval.

## Approved decisions (client sign-off)
- Stack: React SPA + minimal FastAPI backend (single POST /api/enquiry endpoint, no enquiry database)
- Email: Resend (platform-managed), ENQUIRY_DESTINATION=info@nachieng.co.uk env var, form ships DISABLED until end-to-end delivery test; on-screen confirmation only, no auto-reply
- Analytics: none at launch (no GA, no consent banner)
- Calendly: styled external link, not embedded widget
- Design: "The Engineer's Drawing" — paper #F4F3F0 / ink #101216, Signal Blue #1D44C8, steel accent, Archivo (variable, expanded display) + IBM Plex Mono (labels/metadata only), subtle kinetic masked hero, numbered chapters, NO marquee, NO parallax, reduced-motion safe, performance budget wins over decoration
- Logo: client uploads existing logo for assessment; plain-text wordmark placeholder until then
- Canonical host: nachieng.co.uk (non-www)

## User personas
- Maintenance/engineering managers in manufacturing (primary)
- Operations directors, plant managers
- Warehouse/logistics/distribution managers
- Facilities managers (commercial/industrial, hospitality)
- Reliability/CI engineers; organisations interested in maintenance digitisation

## Architecture
- frontend/: React + React Router + Tailwind + framer-motion (LazyMotion) + lenis; content as structured data (src/content/site.js); Seo component (per-page title/meta/canonical/OG/JSON-LD); self-hosted fonts (src/fonts/)
- backend/: FastAPI, enquiry endpoint deferred to Phase 8
- Git: dev branch → preview; main = production, merge only on explicit client approval; documented rollback

## Implemented
- 2026-09-09 Phase 3+4 (preview): Global shell (sticky header, 5-item nav with distinct Contact CTA, accessible mobile overlay menu w/ focus trap + Escape, skip link, footer with reg no/contact/LinkedIn/legal/auto-year), 404 page, homepage with 8 numbered chapters (hero w/ kinetic masked reveal, Tier 1 services, Tier 2 improvement, TMMS teaser "IN DEVELOPMENT", credibility facts, six target environments w/ healthcare flagged "Future target sector", founder + 3 credentials + separate experience statement, insights index, enquiry CTA), home JSON-LD (Organization/LocalBusiness/Person), stub pages for all remaining routes with phase labels. PostHog template tracking removed.
- 2026-09-10 Phase 5 (preview, awaiting client review): Full Services page — intro, five service blocks (Maintenance, Controls & rotating equipment, Improvement, Advisory, Installation support) each with numbered scope items + "Scope boundary" statement (phrased as competence/scope limits WITHOUT naming prohibited services like EICR/domestic/F-Gas, per strict reading of the do-not-mention rule), target environments grid, 3-step "What happens when you enquire", 6-question FAQ (Radix accordion), JSON-LD Service + FAQPage + BreadcrumbList. Shared components extracted: Chapter, IndustryGrid, PrimaryButton, EnquiryCta (homepage refactored to use them).

- 2026-09-10 Client corrections applied: credential 3 is now exactly "18th Edition electrical qualification" (no "currently held and valid"); HV Authorised Person confirmed absent everywhere (never published); founder bio replaced with client-approved exact wording (single source: FOUNDER_BIO in content/site.js); UK English.
- 2026-09-10 Phase 7 (preview, awaiting client review): About page — intro, Founder (approved bio, 3 corrected credentials, separate experience statement), The company (facts grid + service-area note), Approach (scope before work / direct accountability / evidence on request), enquiry CTA, JSON-LD AboutPage + Person + BreadcrumbList.
- 2026-09-10 Logo received: "Nachi Eng Oficial Logo-white background.png" — 363×207 px PNG, NO transparency (white bg baked in), glossy gear+spanner + wordmark + "Maintenance Management Solutions" tagline. Assessment delivered to client: not usable as production master; controlled refinement proposed (vector wordmark, tagline detached, icon simplified/dropped). AWAITING client decision — no changes made, placeholder wordmark remains. No trademark-clearance claim.
- 2026-09-10 Original articles recovered read-only from public GitHub repo Godswill93/nachieng-website (what-is-a-cmms.html, reactive-vs-preventive-maintenance.html, blog.html). Claim audit delivered; unsourced stats flagged (3x / 3–5x / £5,000 per hour / 40% / boiler price table), plus two company-specific claims that exceed approved facts (healthcare work experience; "we implement for NHS trusts…"). AWAITING source-or-remove decisions before Phase 9 build. Article slugs: /insights/what-is-a-cmms, /insights/reactive-vs-preventive-maintenance.

- 2026-09-10 Client sign-offs received: About page approved with two wording fixes (applied: "engineer responsible for the work"; spaced em dash in founder bio). Footer now "© 2025–{currentYear}". Article stats decision: remove all unsourced figures (3x, 20+ assets, 3–5x, £5,000/hr, 40%, boiler price table, "studies show") and replace with approved qualitative wording; Article 2: two approved replacement sentences (experience; "TMMS is being developed to support… in future, healthcare estates"). Demo integration approach: restyled static HTML in sandboxed frames (Option A). Logo: controlled refinement based on header treatment (Option A).
- 2026-09-10 Phase 6 TMMS (preview, awaiting combined client review): /tmms page (intro "In development", 01 why it is being built [approved qualitative cost wording], 02 nine module areas covered by the demos, 03 Walkthrough section, 04 four simulated sector demos, 05 development status facts, 06 enquiry CTA; JSON-LD SoftwareApplication + Breadcrumb). Walkthrough = reusable WalkthroughSection/WalkthroughMedia (content config in content/tmms.js WALKTHROUGH.items with published flag; custom play/pause/replay/mute/fullscreen controls; captions track + transcript toggle; IntersectionObserver lazy-load; reduced-motion → poster only; static fallback when no sources; section hidden entirely in production when nothing published; preview placeholders gated by REACT_APP_PREVIEW_MEDIA=true in frontend/.env — must be absent/false in production). Four demos recovered read-only from GitHub → /app/memory/legacy, transformed by /app/scripts/build_demos.py → /frontend/public/demos/{factory,warehouse,hotel,hospital}.html (fully self-contained: fonts inlined as data URIs, zero external requests; jsPDF CDN removed → Export PDF shows toast; Google Fonts removed; NachiCMMS→TMMS; emoji stripped; Live badge → "Simulated demonstration — sample data"; CTA banner links to nachieng.co.uk removed; NHS Trusts sentence removed; real name/email in settings replaced with fictional; sector accent colours mapped to Signal Blue; radii flattened; robots noindex). Demo routes /tmms/demos/:sector in React shell (back link, sim banner, reset button, iframe sandbox="allow-scripts" only, referrerPolicy no-referrer, other-demos grid); unknown sector → 404. Note: preview proxy sends Cross-Origin-Resource-Policy: same-origin, so opaque-origin frames cannot fetch sibling assets — that is why everything is inlined.
- 2026-09-10 Logo refinement produced (NOT yet applied — awaiting approval): /app/scripts/build_brand.py generates /frontend/public/brand/: wordmark ink/paper SVG (Archivo 700/wdth 118 outlines + Plex Mono 500 "LTD" at 55%, 0.18em tracking), stacked variants, N mark (paper/ink tile, two ink uprights + Signal Blue diagonal), favicon.svg (auto dark-mode swap), apple-touch-icon, PNG exports 2400/1600/1024/512/192/180/32. Preview at /brand-preview (noindex, unlinked).
- 2026-09-10 All public-facing build messages ("Phase X", "build queue", "rebuild") removed from stub pages and 404. Testing agent iteration_1: 100% pass (frontend).
- 2026-06 (this batch) Phase 8 Contact + enquiry pipeline (preview): real ContactPage form (accessible labels, inline validation, required consent checkbox, on-page privacy notice stating no database/no marketing, honeypot + min-fill-time + per-IP rate-limit spam protection, success/error states, Calendly + email/phone). Backend POST /api/enquiry via Emergent-managed Resend (server-side template, escaped fields, guardrail gate _assert_safe_email on every send, from_name="Nachi Eng Ltd"). PRODUCTION SUBMISSION DISABLED: ENQUIRY_LIVE=false routes all sends to delivered@resend.dev (proven: two 202 Accepted). Real destination info@nachieng.co.uk held behind the OFF switch (env ENQUIRY_DESTINATION) — flip ENQUIRY_LIVE=true only after client's own delivery test. No auto-reply, no DB.
- 2026-06 (this batch) Phase 9 Insights (preview): content/insights.js (structured blocks) + InsightsPage index + ArticlePage (/insights/:slug). Both recovered articles published with corrections applied — removed all unsupported stats/monetary figures (3x, 3–5x, £5,000/hr, 40%, 20+ assets, boiler price table), "studies show", all emojis, NHS Trust + CQC/HTM specifics + "we implement for NHS trusts" claim; Article 2 closing rewritten to approved wording (hands-on experience; TMMS "in development … in future, healthcare estates", no deployment claimed). Each article has route, title, meta description, canonical, date ("May 2026" preserved), reading time, internal links (/tmms, /contact, cross-link) + BlogPosting JSON-LD. Home + footer article links updated to real slugs.
- 2026-06 (this batch) Demo guided tour: components/tmms/DemoTour.jsx — first-visit 3-step intro card on each /tmms/demos/:sector page. Clearly labelled "Simulated demonstration — a quick tour" + "sample data only, nothing live/real"; dismissible (X/skip/backdrop), skippable, Back/Next, keyboard accessible (focus to close, Tab trap, Escape), remembered per-sector via localStorage (nachi-demo-tour-{sector}), SUPPRESSED entirely when prefers-reduced-motion, responsive, non-blocking after dismissal, no real data. Verified rendering + suppression behaviour on preview.
- 2026-06 Logo: refined wordmark + N mark variants remain staged at /brand-preview only (single-line light/dark, stacked, favicon small sizes, transparent SVG + PNG exports). NOT swapped into header/footer/favicon — awaiting explicit client visual approval.
- 2026-06 (batch 2, client-approved) Logo APPLIED: header uses /brand/nachi-eng-wordmark-ink.svg (light bg) + paper svg in mobile menu; footer uses paper svg; index.html now links favicon.svg + favicon-32.png + apple-touch-icon.png. Approved lockups used exactly, no new concepts. Plain-text wordmark removed. Testing agent iteration_3: 100% (naturalWidth 816, assets 200).
- 2026-06 (batch 2) Legal DRAFTS published for owner review: PrivacyPage + CookiePage (routes /privacy, /cookies; replaced stubs). Based ONLY on processors currently used — Resend (email delivery) + Calendly (external booking). Cookie policy states no analytics/advertising cookies; notes the demo-tour localStorage flag (not a cookie); no invented processors/cookies/AI processing. Both carry a "DRAFT FOR OWNER REVIEW" banner (data-testid legal-draft-banner).
- 2026-06 (batch 2) Article sharing: OG/LinkedIn preview images generated (on-brand paper/ink line art, no text/stats) at /brand/og-what-is-a-cmms.jpg + /brand/og-reactive-vs-preventive.jpg; Seo component extended with image -> og:image/twitter:image + summary_large_image; copy-link button (data-testid article-copy-link) on both articles copies the canonical URL. May 2026 dates + corrected wording preserved.
- 2026-06 Enquiries remain OFF (ENQUIRY_LIVE=false) — preview routes to delivered@resend.dev only; no real email sent to info@nachieng.co.uk. Awaiting client's separate activation approval after their own delivery test.
- 2026-06 (batch 3) WCAG 2.2 AA accessibility pass (Phase 12): axe-core 4.10.2 audit across 11 routes now 0 violations (iteration_6). Fixes: /insights <ul>/<li> semantics (Reveal moved inside li); site-wide muted-text contrast bumped (text-ink/50,/45,/40 → /70,/60; footer/home/EnquiryCta text-white/40 → /60) to meet 4.5:1; input focus-visible ring; article comparison table now uses th scope=col/row; mobile contact testid de-duplicated (mobile-nav-contact). Keyboard verified: skip link, nav aria-current, mobile-menu focus trap + Escape, contact inline role=alert errors, demo-tour Tab trap + Escape, article copy-link. /brand-preview stale "not yet applied" status corrected to "applied" (route stays noindex/unlinked).
- 2026-06 (batch 4) TMMS Demonstration Lab: new top-level page /demonstrations (nav item "Demonstrations" + cross-link from /tmms via tmms-demo-lab-link). Public naming enforced: "TMMS — Maintenance Management System" / "by Nachi Eng Ltd" / "in the CMMS category"; positioned as configurable (not a finished per-sector product), in development, sample data. NO "Taiko" anywhere (grep + rendered check clean). Four sector blocks (factory/warehouse/hotel/hospital, hospital = future target) each with category, "Simulated demonstration — sample data" label, "what this walkthrough will show" interactions, and "Open the live simulated demonstration" → /tmms/demos/{slug}. Reuses components/tmms/WalkthroughMedia (lazy IntersectionObserver, poster, captions track, transcript, play/pause/replay/mute/fullscreen, reduced-motion fallback, published/unpublished visibility, static fallback, no autoplay, no upload form). MEDIA UNPUBLISHED: no approved MP4/WebM assets yet — browser recording (Playwright) not available in this environment, so per instruction the component ships media-ready with clearly-marked unpublished placeholders (hidden in production) and STOPS for asset approval. No AI video provider connected; no extra sectors added. Testing iteration_7: 52/52 pass, axe 0 violations on /demonstrations.
- 2026-06 (batch 4) Launch prep: /app/memory/LAUNCH_CHECKLIST.md drafted (redirects/301s, sitemap, robots, canonical, OG checks, favicon checks, enquiry form config, rollback). PREPARATION ONLY — no deploy, no domain/DNS change. Legal pages re-reviewed, kept as DRAFT (Resend + Calendly only). Enquiries still OFF (ENQUIRY_LIVE=false). Social-preview recheck deferred until production domain connected.
- 2026-06 (batch 5) Legal wording revised (still DRAFT): Privacy lawful basis changed to legitimate interest only (no consent-as-basis); added voluntary/required-fields note; exact 12-month retention wording; address reduced to "England & Wales, Company No. 16567818" + email (no street); added Resend US international-transfer statement (UK Addendum to EU SCCs + UK Extension to EU–U.S. DPF, per Resend's published terms); full 8-point UK GDPR rights list incl. withdraw-consent-where-used + ICO. Cookie policy: verified no analytics/advertising/tracking cookies + no other storage/pixels/analytics/marketing beyond the essential TMMS-tour localStorage flag; Calendly cookies only after leaving site; no consent banner.
- 2026-06 (batch 7) Enquiry backend migrated to a same-origin Vercel Node.js Function at frontend/api/enquiry.js (deploys as POST /api/enquiry when Vercel Root=frontend). Production no longer depends on the Emergent preview backend or MongoDB; enquiry data is not stored in any DB and enquiry content/personal data is never logged (only HTTP status on failure). Implements POST+JSON only, 100KB body cap, field-length limits, required name/email/message, optional org/phone, honeypot + <1500ms timing drops (silent 200, no send), header/HTML-injection rejection, escaped HTML + plain-text body, generic public errors, 20s timeout, Resend server-side via RESEND_API_KEY, reply_to = validated enquirer email, ENQUIRY_LIVE=false → delivered@resend.dev. Rate limiting intentionally omitted (best-effort honeypot+timing only). New env names: RESEND_API_KEY, EMAIL_FROM, EMAIL_FROM_NAME, EMAIL_REPLY_TO, ENQUIRY_DESTINATION, ENQUIRY_TEST_DESTINATION, ENQUIRY_LIVE. Frontend ContactPage now posts to same-origin /api/enquiry (REACT_APP_BACKEND_URL no longer referenced in src). Tested 20/20 focused function tests; production yarn build succeeds emitting sitemap.xml/robots.txt/brand assets. No secret literals committed; .env untracked+ignored. IMPORTANT: EMERGENT_EMAIL_KEY is not a Resend key — owner must create their own Resend API key; production EMAIL_FROM must be on a Resend-verified domain (DNS later). Nothing deployed.
- 2026-06 (batch 6) Vercel-readiness: canonical production domain switched to https://www.nachieng.co.uk everywhere (site.js canonicalOrigin drives canonical + og:url + og:image + JSON-LD; static index.html canonical/og:url updated; sitemap uses www) — no mixed www/non-www. Homepage entrance sped up (KineticLines h1 duration 0.5 + delay 0.04+i*0.08 → h1 in ≤~0.62s; Reveal gained duration prop; hero lead/CTA delay 0.18/0.26 duration 0.4) so top is not blank on load; reduced-motion still shows everything. Contact nav active-state fixed: desktop nav-contact-cta and mobile links converted to NavLink → aria-current='page' on /contact (desktop+mobile). Added public/robots.txt (Allow /, Disallow /brand-preview + /tmms/demos/, Sitemap www) and public/sitemap.xml (11 indexable www routes; excludes /brand-preview + /tmms/demos). Hardened .gitignore to ignore frontend/.env + backend/.env (secrets never committed; none tracked). Testing iteration_9: 100% pass; axe 0 violations on / and /contact (desktop+mobile). Enquiries still OFF; nothing deployed.

## Recorded for later (NOT built in this batch — client instruction)
### Future AI Operations Model (Contact / Privacy / operations requirement)
- Customer-facing agent personas use professional names + role titles, NOT led by the word "AI". Initial roles: Nia — Client Enquiries Manager; Sales Coordinator; Scheduling Coordinator; TMMS Demonstration Guide; Operations and Reporting Manager.
- Approved opening line: "Hello, you're speaking with Nia, NACHI ENG's Client Enquiries Manager. I can help with enquiries, bookings and information. How can I help you today?"
- Agents must NOT claim to be human engineers, must NOT claim qualifications they do not hold, and must answer truthfully if directly asked whether they are automated.
- All future AI agents must use least-privilege access, encryption, identity verification, audit logs, redaction, approval gates and strict UK GDPR controls.
- Status: recorded only. Do not implement until separately approved.
## Backlog (phased per client)
- P0: LOGO APPROVAL → then apply wordmark SVG to header/footer, favicon set + manifest in index.html, OG 1200×630 image
- P0: Enquiry go-live — after client's own delivery test to info@nachieng.co.uk, set ENQUIRY_LIVE=true (backend/.env) + restart backend
- P1: Phase 10 legal drafts; Phase 11 responsive pass (incl. minor sample-data badge overflow inside demo frames on narrow mobile — original demo CSS); Phase 12 accessibility pass
- P2: Phase 13 SEO pass (redirects incl. 410 for /nachi-invoice-generator.html, sitemap, robots.txt); Phase 14 performance pass (font preload, bundle audit); Phase 15 testing; Phase 16 deployment-prep checklist (NO deploy without explicit instruction; REACT_APP_PREVIEW_MEDIA must be unset in production)
- Walkthrough media: awaiting client-approved posters + MP4/WebM clips + captions/transcripts; set published:true per item in content/tmms.js

## Known constraints / notes
- Never modify DNS, Vercel, email records, existing GitHub repo, or the live site
- Resend sending domain verification requires client-side DNS changes at Phase 8
- Kinetic hero must stay subtle/mobile-safe; drop motion before budget
